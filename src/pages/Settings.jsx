import React from "react";
export default function Settings({monitoring,onToggle,onClear}){return <div className="page"><div className="section-heading"><div><h2>Settings</h2><p>Configure monitoring behaviour for the current session.</p></div></div><section className="glass settings-card"><Row title="Real-time monitoring" text="Generate live events and update the SOC dashboard automatically." on={monitoring} onClick={onToggle}/><Row title="Severity detection engine" text="Pattern-based detection for authentication failures, scans, restricted files and privilege escalation." on={true}/><div className="danger-row"><div><b>Clear session data</b><span>Remove all analyzed logs and reset the dashboard.</span></div><button className="danger-btn" onClick={onClear}>Clear Data</button></div></section></div>}
function Row({title,text,on,onClick}){return <div className="setting-row"><div><b>{title}</b><span>{text}</span></div><button className={`switch ${on?"on":""}`} onClick={onClick}><i></i></button></div>}