import React,{useState} from "react";import {BellRing,ShieldAlert} from "lucide-react";import SeverityBadge from "../components/SeverityBadge";
export default function Alerts({logs}){const [sev,setSev]=useState("all");const rows=logs.filter(l=>l.severity!=="normal"&&(sev==="all"||l.severity===sev));return <div className="page"><div className="section-heading"><div><h2>Security Alerts</h2><p>Investigate everything flagged as suspicious or worse.</p></div><select className="compact-select" value={sev} onChange={e=>setSev(e.target.value)}><option value="all">All alerts</option><option value="medium">Medium</option><option value="high">High</option><option value="critical">Critical</option></select></div><div className="alert-banner glass"><ShieldAlert/><div><b>{rows.length} active findings</b><span>Review high and critical events first.</span></div></div><section className="glass table-card"><div className="table-wrap"><table><thead><tr><th>TIME</th><th>IP</th><th>DETECTION</th><th>SEVERITY</th><th>RECOMMENDATION</th></tr></thead><tbody>{rows.slice(0,500).map(l=><tr key={l.id}><td className="mono">{l.time}</td><td className="mono strong">{l.ip}</td><td><b>{l.event}</b><div className="muted">{l.details}</div></td><td><SeverityBadge severity={l.severity}/></td><td className="muted">{l.severity==="critical"?"Investigate immediately and isolate the source if confirmed.":"Review source activity and correlate with related events."}</td></tr>)}</tbody></table></div></section></div>}