import React from "react";import SeverityBadge from "../components/SeverityBadge";
export default function IPAnalysis({logs}){const map={};logs.forEach(l=>{map[l.ip]??={ip:l.ip,total:0,normal:0,medium:0,high:0,critical:0};map[l.ip].total++;map[l.ip][l.severity]++});const rows=Object.values(map).sort((a,b)=>b.total-a.total);return <div className="page"><div className="section-heading"><div><h2>IP Analysis</h2><p>Source IPs ranked by event volume and observed risk.</p></div></div><section className="glass table-card"><div className="table-wrap"><table><thead><tr><th>IP ADDRESS</th><th>TOTAL</th><th>NORMAL</th><th>MEDIUM</th><th>HIGH</th><th>CRITICAL</th><th>RISK</th></tr></thead><tbody>{rows.map(r=>{const level=r.critical?"critical":r.high?"high":r.medium?"medium":"normal";return <tr key={r.ip}><td className="mono strong">{r.ip}</td><td>{r.total}</td><td>{r.normal}</td><td>{r.medium}</td><td>{r.high}</td><td>{r.critical}</td><td><SeverityBadge severity={level}/></td></tr>})}</tbody></table></div></section></div>}