import React,{useMemo,useState} from "react"; import SeverityBadge from "../components/SeverityBadge";
export default function Logs({logs,query}){const [sev,setSev]=useState("all");const rows=useMemo(()=>logs.filter(l=>(sev==="all"||l.severity===sev)&&(!query||`${l.ip} ${l.event} ${l.details}`.toLowerCase().includes(query.toLowerCase()))),[logs,sev,query]);return <div className="page"><div className="section-heading"><div><h2>View Logs</h2><p>Browse, search and filter every parsed security record.</p></div><span className="record-count">{rows.length} records</span></div><div className="glass filterbar"><input value={query||""} readOnly placeholder="Search IP, event or details"/><select value={sev} onChange={e=>setSev(e.target.value)}><option value="all">All severities</option><option value="normal">Normal</option><option value="medium">Medium</option><option value="high">High</option><option value="critical">Critical</option></select></div><section className="glass table-card"><div className="table-wrap"><table><thead><tr><th>TIME</th><th>IP ADDRESS</th><th>EVENT</th><th>SEVERITY</th><th>DETAILS</th></tr></thead><tbody>{rows.slice(0,500).map(l=><tr key={l.id}><td className="mono">{l.time}</td><td className="mono strong">{l.ip}</td><td>{l.event}</td><td><SeverityBadge severity={l.severity}/></td><td className="muted">{l.details}</td></tr>)}</tbody></table></div></section></div>}