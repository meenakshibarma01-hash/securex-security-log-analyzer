import React from "react";import {FileDown,FileJson,Table2,Printer,LockKeyhole} from "lucide-react";import {exportCSV,exportJSON,exportHTMLReport} from "../utils/report";
export default function Reports({logs}){return <div className="page"><div className="section-heading"><div><h2>Security Reports</h2><p>Export your completed analysis for presentation or investigation.</p></div></div><div className="report-lock glass"><LockKeyhole/><div><b>Report export is enabled</b><span>Analysis contains {logs.length.toLocaleString()} processed records.</span></div></div><div className="report-grid"><ReportCard icon={Printer} title="Printable PDF Report" text="Open a professionally formatted report and save it as PDF." onClick={()=>exportHTMLReport(logs)}/><ReportCard icon={Table2} title="CSV Export" text="Export all analyzed records for Excel and investigation workflows." onClick={()=>exportCSV(logs)}/><ReportCard icon={FileJson} title="JSON Export" text="Download structured log data for further processing." onClick={()=>exportJSON(logs)}/></div></div>}
function ReportCard({icon:Icon,title,text,onClick}){return <button className="glass report-card" onClick={onClick}><div className="report-icon"><Icon/></div><div><b>{title}</b><p>{text}</p></div><FileDown size={18}/></button>}